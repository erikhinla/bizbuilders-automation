// Script to handle office video backgrounds
// Since office files are MP4, we'll use them as video backgrounds instead of static images

// Note: CSS background-image doesn't support video files
// Options:
// 1. Extract frames from videos to create static images (requires ffmpeg)
// 2. Use video elements as backgrounds (better, more dynamic)
// 3. Use a canvas approach to display video frames as background

// For now, we'll update the HTML to use video elements for office backgrounds
// which creates a more dynamic, premium effect

console.log('Office videos detected - will use as video backgrounds for dynamic effect!');

